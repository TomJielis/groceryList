export default defineNuxtRouteMiddleware((to, from) => {

    // const { user } = useAuth();
    // console.log(user);
    // if (!user.value) {
    //     return navigateTo('/auth/login');
    // }
});